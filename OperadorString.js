// Operador de String (String operator)

// comparison (comparação)
//console.log('a' == 'a')

// concatenation (concatenação)
// Retorna a união de duas Strings
let alpha1 = 'alpha'
let alpha2 = 'alpha'
console.log(alpha1 += 'bet') 
//or == ou
console.log(alpha2 + 'bet')
